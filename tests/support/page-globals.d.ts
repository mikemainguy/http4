// Brings the page's window.__echo / window.__http4 declarations (from
// client/src/main.ts) into the test project, so page.evaluate callbacks are typed.
import type {} from "../../client/src/main.ts";

{
  "name": "http4",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild client/src/main.ts --bundle --format=esm --target=chrome120 --sourcemap --outdir=client/dist",
    "typecheck": "tsc -p client && tsc -p tests",
    "test:go": "cd server && go test ./...",
    "test": "npm run typecheck && npm run test:go && npm run test:wire && npm run test:client && npm run test:browser",
    "test:wire": "node --test tests/wire/wire.test.ts",
    "test:client": "node --test \"tests/client/*.test.ts\"",
    "test:browser": "npm run build && node --test --test-concurrency=1 tests/echo/echo.test.ts tests/fetch/fetch.test.ts tests/loss/loss.test.ts tests/meta/meta.test.ts tests/impair/impair.test.ts tests/h3/h3.test.ts tests/bdp/bdp.test.ts",
    "fixtures": "scripts/gen-fixtures",
    "integrity": "npm run build && node tests/integrity/run.ts"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "esbuild": "^0.28.2",
    "playwright-core": "^1.63.0",
    "typescript": "^7.0.2"
  }
}
